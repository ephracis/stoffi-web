description "Fast web server & app server, Ruby Python Node.js - Phusion Passenger"

start on filesystem and static-network-up
stop on runlevel [016]

respawn

pre-start script
  cd <%= fetch(:deploy_to) %>/current
end script

exec ~/.rvm/bin/rvm 2.1.2 do /usr/bin/passenger start --port <%= fetch(:passenger_port, 4000) %> \
	--environment <%= fetch(:rails_env) %> --ssl \
	--ssl-certificate /etc/ssl/<%= fetch(:application) %>_<%= fetch(:stage) %>.cert \
	--ssl-certificate-key /etc/ssl/<%= fetch(:application) %>_<%= fetch(:stage) %>.key