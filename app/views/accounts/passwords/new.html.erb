<div class="panel panel-default panel-small center-block" id='password'>
  <div class="panel-heading">
    <h3 class="panel-title"><%= t 'accounts.password.title' %></h3>
  </div>
  <div class="panel-body">
    
		<%= form_for resource, as: resource_name,
		  url: password_path(resource_name) do |f| %>
	
			<%= render 'shared/messages' %>
			
		  <div class="input-group">
        <span class="input-group-addon" id="password-email">
          <span class='fa fa-envelope'></span>
        </span>
        <%= f.email_field :email, autofocus: true, class: 'form-control',
          placeholder: t("accounts.login.email"),
          aria_describedby: :password_email %>
      </div>
      
      <div class='text-right'>
  		  <%= f.button class: 'btn btn-primary' do %>
  				<span class='fa fa-key'></span>
  				<%=t 'accounts.password.button' %>
  			<% end %>
  		</div>
			
		<% end %>
			
		<%= render 'shared/login/links' %>
		
	</div>
</div>