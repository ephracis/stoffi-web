<%
all_categories = @search.categories_array.length == Search.categories.length
%>
<%= @results[:hits].map do |hit|
	if params.keys.include? 'simple'
		hit.display
	else
  		render partial: hit, locals: { show_badge: all_categories }
	end
end.to_s.html_safe
%>