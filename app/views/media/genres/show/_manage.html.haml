- if current_user.admin?
  .panel.panel-default
    .panel-heading
      %span.fa.fa-fw.fa-shield
      = t 'media.genres.manage.heading'
    .panel-body
  
      = link_to edit_genre_path(@genre),
          class: 'btn btn-default', target: '_self' do
        %span.fa.fa-fw.fa-edit
        = t 'media.genres.manage.edit'
      
      = link_to genre_path(@genre),
          class: 'btn btn-danger', method: :delete, target: '_self',
          data: { confirm: t('media.genres.manage.confirm') } do
        %span.fa.fa-fw.fa-remove
        = t 'media.genres.manage.remove'
